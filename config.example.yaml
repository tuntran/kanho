server:
  host: "0.0.0.0"
  port: 8080

database:
  url: "postgres://kanho:kanho@localhost:5432/kanho?sslmode=disable"

jwt:
  secret: "change-me-in-production"
  access_ttl: "15m"
  refresh_ttl: "168h"

storage:
  endpoint: "localhost:9000"
  bucket: "kanho"
  access_key: "minioadmin"
  secret_key: "minioadmin"
  use_ssl: false
  public_url: "http://localhost:9000/kanho"
